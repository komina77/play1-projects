# Application dependencies

require:
    - play 1.5.3
    - play -> docviewer
    - localLibrary  -> launcher 0.1.1


repositories:
# module
    - localLibrary:
        type:       local
        artifact:   ${application.path}/../../dist/[module]-[revision].zip
        contains:
            - localLibrary -> *

 